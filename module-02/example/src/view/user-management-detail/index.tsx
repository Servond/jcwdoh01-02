export default function UserManagementDetailView({ slug }: { slug: string }) {
  return (
    <div className="flex flex-col justify-center items-center my-10">
      Detail email {slug}
    </div>
  );
}
